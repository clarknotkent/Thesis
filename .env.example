PORT=5055
# Dev: set to Vite's address so CORS works during development.
# Production (coupled/built): ORIGIN is not needed since frontend is served by Express.
ORIGIN=http://localhost:5173
APP_SECRET=change_me_dev_secret
