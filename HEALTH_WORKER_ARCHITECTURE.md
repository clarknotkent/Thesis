# Health Worker Chat & Notifications - System Architecture

## 🏗️ Overall Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     Health Worker Frontend                       │
│                                                                   │
│  ┌──────────────────────┐      ┌──────────────────────┐        │
│  │   Notifications.vue  │      │     Messages.vue     │        │
│  │                      │      │                      │        │
│  │  - Filter tabs       │      │  - Conversations     │        │
│  │  - Notification list │      │  - Chat interface    │        │
│  │  - Actions           │      │  - New conversation  │        │
│  │  - Polling (30s)     │      │  - Polling (3s)      │        │
│  └──────────┬───────────┘      └──────────┬───────────┘        │
│             │                              │                     │
│             └──────────────┬───────────────┘                     │
│                            │                                     │
│                   ┌────────▼────────┐                           │
│                   │   api.js        │                           │
│                   │  - notificationAPI                          │
│                   │  - conversationAPI                          │
│                   │  - messageAPI                               │
│                   └────────┬────────┘                           │
└────────────────────────────┼─────────────────────────────────────┘
                             │
                             │ HTTP/HTTPS
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│                        Backend (Node.js)                          │
│                                                                   │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ Notification    │  │ Conversation    │  │   Message       │ │
│  │   Routes        │  │    Routes       │  │   Routes        │ │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘ │
│           │                    │                     │          │
│  ┌────────▼────────┐  ┌────────▼────────┐  ┌────────▼────────┐ │
│  │ Notification    │  │ Conversation    │  │   Message       │ │
│  │  Controller     │  │   Controller    │  │  Controller     │ │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘ │
│           │                    │                     │          │
│  ┌────────▼────────┐  ┌────────▼────────┐  ┌────────▼────────┐ │
│  │ Notification    │  │ Conversation    │  │   Message       │ │
│  │    Model        │  │     Model       │  │    Model        │ │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘ │
│           │                    │                     │          │
│           └────────────────────┼─────────────────────┘          │
│                                │                                 │
└────────────────────────────────┼─────────────────────────────────┘
                                 │
                                 │ SQL
                                 │
┌────────────────────────────────▼─────────────────────────────────┐
│                      Database (PostgreSQL)                        │
│                                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │
│  │  notifications   │  │  conversations   │  │   messages    │ │
│  │                  │  │                  │  │               │ │
│  │ - id             │  │ - id             │  │ - id          │ │
│  │ - user_id        │  │ - subject        │  │ - conv_id     │ │
│  │ - template_code  │  │ - created_by     │  │ - sender_id   │ │
│  │ - message_body   │  │ - created_at     │  │ - content     │ │
│  │ - channel        │  │                  │  │ - created_at  │ │
│  │ - read_at        │  └──────────────────┘  │ - read_at     │ │
│  │ - created_at     │                        └───────────────┘ │
│  └──────────────────┘  ┌──────────────────┐  ┌───────────────┐ │
│                        │ conversation_    │  │  message_     │ │
│                        │  participants    │  │  receipts     │ │
│                        │                  │  │               │ │
│                        │ - conversation_id│  │ - message_id  │ │
│                        │ - user_id        │  │ - user_id     │ │
│                        │ - joined_at      │  │ - read_at     │ │
│                        │ - left_at        │  │ - delivered_at│ │
│                        └──────────────────┘  └───────────────┘ │
└───────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### Notifications Flow

```
┌──────────────┐
│ User Opens   │
│ Notifications│
└──────┬───────┘
       │
       ▼
┌──────────────────────────┐
│ loadNotifications()      │
│ GET /api/notifications   │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Backend fetches from DB  │
│ - Filter by user_id      │
│ - Order by created_at    │
│ - Include read status    │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Transform data:          │
│ - Map notification types │
│ - Format timestamps      │
│ - Set read/unread status │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Display in UI:           │
│ - Group by filter        │
│ - Show unread badges     │
│ - Color code by type     │
└──────────────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Start polling (30s)      │
│ Auto-refresh in bg       │
└──────────────────────────┘

User Actions:
├─ Click notification ──► Mark as read ──► PUT /api/notifications/:id/read
├─ Delete notification ─► Delete ──────► DELETE /api/notifications/:id
├─ Mark all read ───────► Bulk update ─► Multiple PUT requests
└─ Clear all read ──────► Bulk delete ─► Multiple DELETE requests
```

### Messages Flow

```
┌──────────────┐
│ User Opens   │
│   Messages   │
└──────┬───────┘
       │
       ▼
┌──────────────────────────┐
│ loadConversations()      │
│ GET /api/conversations   │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Backend:                 │
│ - Join participants      │
│ - Get latest messages    │
│ - Calculate unread count │
│ - Enrich with user data  │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Display conversations:   │
│ - Show participants      │
│ - Preview last message   │
│ - Show unread badge      │
│ - Relative timestamps    │
└──────────────────────────┘

User clicks conversation:
       │
       ▼
┌──────────────────────────┐
│ openConversation()       │
│ GET /api/messages/:id    │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Backend:                 │
│ - Fetch all messages     │
│ - Mark as delivered/read │
│ - Order by timestamp     │
│ - Include sender info    │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Display chat:            │
│ - Bubble layout          │
│ - Your vs their messages │
│ - Auto-scroll to bottom  │
│ - Start polling (3s)     │
└──────────────────────────┘

User sends message:
       │
       ▼
┌──────────────────────────┐
│ handleSendMessage()      │
│ POST /api/messages       │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Backend:                 │
│ - Create message record  │
│ - Create receipts        │
│ - Update conversation    │
│ - Trigger notifications  │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Refresh messages         │
│ Scroll to new message    │
└──────────────────────────┘

New conversation:
       │
       ▼
┌──────────────────────────┐
│ createConversation()     │
│ POST /api/conversations  │
│      /start              │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Backend:                 │
│ - Create conversation    │
│ - Add participants       │
│ - Send first message     │
│ - Notify participants    │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Open new conversation    │
│ in chat view             │
└──────────────────────────┘
```

---

## 🔄 Polling Mechanism

### Notifications Polling (30 seconds)
```
┌─────────────────────────────────────────┐
│  Component Mounted                       │
└───────────┬─────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│  Initial load: loadNotifications()       │
└───────────┬─────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│  startPolling()                          │
│  setInterval(() => {                     │
│    loadNotifications()                   │
│  }, 30000)                               │
└───────────┬─────────────────────────────┘
            │
            ▼
    ┌───────────────┐
    │  Every 30s    │
    └───────┬───────┘
            │
            ▼
┌─────────────────────────────────────────┐
│  Silent background refresh               │
│  - No loading spinner                    │
│  - Update notification list              │
│  - Maintain user's current filter        │
└───────────┬─────────────────────────────┘
            │
            │  Component Unmounting
            ▼
┌─────────────────────────────────────────┐
│  stopPolling()                           │
│  clearInterval(pollInterval)             │
└─────────────────────────────────────────┘
```

### Messages Polling (3 seconds)
```
┌─────────────────────────────────────────┐
│  User selects conversation               │
└───────────┬─────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│  openConversation(conv)                  │
│  - Load initial messages                 │
│  - Set selectedConversation              │
└───────────┬─────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│  watch(selectedConversation)             │
│  - Stop old polling                      │
│  - Start new polling                     │
└───────────┬─────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│  setInterval(() => {                     │
│    loadMessages(conv.id)                 │
│  }, 3000)                                │
└───────────┬─────────────────────────────┘
            │
            ▼
    ┌───────────────┐
    │   Every 3s    │
    └───────┬───────┘
            │
            ▼
┌─────────────────────────────────────────┐
│  Smart message refresh:                  │
│  - Check for new messages                │
│  - Only update if count changed          │
│  - Preserve scroll if not at bottom      │
│  - Auto-scroll if at bottom              │
└───────────┬─────────────────────────────┘
            │
            │  User closes conversation
            ▼
┌─────────────────────────────────────────┐
│  watch triggers:                         │
│  - clearInterval(pollInterval)           │
│  - selectedConversation = null           │
└─────────────────────────────────────────┘
```

---

## 🎯 Component Lifecycle

### Notifications Component
```
Mount ──► Load notifications ──► Start polling
  │                                    │
  │                                    ▼
  │                          ┌──────────────────┐
  │                          │  Auto-refresh    │
  │                          │  every 30s       │
  │                          └──────────────────┘
  │                                    │
  ▼                                    │
User interactions:                     │
  - Filter notifications               │
  - Mark as read                       │
  - Delete notification                │
  - Mark all read                      │
  - Clear all read                     │
  │                                    │
  │                                    │
  └────────────────┬───────────────────┘
                   │
                   ▼
           Component unmounts
                   │
                   ▼
              Stop polling
```

### Messages Component
```
Mount ──► Load conversations
  │              │
  │              ▼
  │    Display conversations list
  │              │
  │              ▼
  │    User selects conversation
  │              │
  │              ▼
  │    Load messages + Start polling (3s)
  │              │
  │              ▼
  │    ┌──────────────────────┐
  │    │  Display chat view   │
  │    │  - Message bubbles   │
  │    │  - Input box         │
  │    │  - Auto-refresh      │
  │    └──────────┬───────────┘
  │              │
  │              ▼
  │    User sends message ──► Refresh messages
  │              │
  │              ▼
  │    User clicks back ──► Stop polling
  │              │             │
  │              ▼             ▼
  │    Return to conversations list
  │              │
  └──────────────┴───────────────────────┐
                                         │
                                         ▼
                                 Component unmounts
                                         │
                                         ▼
                                    Stop polling
```

---

## 🔐 Security & Authorization

```
┌──────────────────────────────────────┐
│  Frontend Request                     │
│  - Includes auth token in header     │
│  - Authorization: Bearer <token>     │
└───────────┬──────────────────────────┘
            │
            ▼
┌──────────────────────────────────────┐
│  Backend Middleware                   │
│  - authenticateRequest()              │
│  - checkUserMapping()                 │
│  - Extract user_id from token        │
└───────────┬──────────────────────────┘
            │
            ▼
┌──────────────────────────────────────┐
│  Authorization Check                  │
│  - Verify role = 'healthworker'      │
│  - Verify user owns resource         │
└───────────┬──────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
   ✅ OK         ❌ Deny
     │             │
     ▼             ▼
 Process      Return 401/403
 Request
```

---

## 📦 File Structure

```
faith/Thesis/
├── frontend/
│   ├── src/
│   │   ├── views/
│   │   │   └── healthworker/
│   │   │       ├── Notifications.vue  ← Notifications UI
│   │   │       └── Messages.vue       ← Chat UI
│   │   ├── services/
│   │   │   └── api.js                 ← API client
│   │   └── router/
│   │       └── index.js               ← Route definitions
│   └── ...
├── backend/
│   ├── routes/
│   │   ├── notificationRoutes.js      ← Notification endpoints
│   │   ├── conversationRoutes.js      ← Conversation endpoints
│   │   └── messageRoutes.js           ← Message endpoints
│   ├── controllers/
│   │   ├── notificationController.js
│   │   ├── conversationController.js
│   │   └── messageController.js
│   └── models/
│       ├── notificationModel.js
│       ├── conversationModel.js
│       └── messageModel.js
└── docs/
    ├── HEALTH_WORKER_CHAT_NOTIFICATIONS.md
    ├── HEALTH_WORKER_FEATURES_QUICK_GUIDE.md
    └── HEALTH_WORKER_ARCHITECTURE.md  ← This file
```

---

## 🚀 Deployment Considerations

### Environment Variables
```env
# Polling intervals (milliseconds)
NOTIFICATION_POLL_INTERVAL=30000  # 30 seconds
MESSAGE_POLL_INTERVAL=3000        # 3 seconds

# API limits
MAX_NOTIFICATIONS=100
MAX_MESSAGES_PER_CONVERSATION=50
MAX_CONVERSATIONS=50

# Message constraints
MAX_MESSAGE_LENGTH=1000
```

### Performance Optimization
- Database indexes on user_id, created_at
- Query limits to prevent over-fetching
- Efficient polling intervals
- Client-side caching
- Lazy loading of conversations

### Scalability
- Consider WebSocket for real-time updates
- Implement message pagination
- Add CDN for static assets
- Database connection pooling
- Rate limiting on API endpoints

---

This architecture provides a robust, scalable foundation for health worker communication features! 🎉
